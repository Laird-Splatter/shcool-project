@model IEnumerable<WebApplication2.Models.Filiere>

@{
    ViewBag.Title = "Index";
}
<div class="jumbotron" style="background-color:#add8e6; text-align:center; margin-top:25px; align-content:center;">

    <h2 style="color:red;">Filiere</h2>

    <p>
        @Html.ActionLink("Creé une nouveau Filiere", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nomF)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.secteur.nomSecteur)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nomF)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.secteur.nomSecteur)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.CodeFil }) |
                    @Html.ActionLink("Details", "Details", new { id = item.CodeFil }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.CodeFil })
                </td>
            </tr>
        }

    </table>
    </div>
